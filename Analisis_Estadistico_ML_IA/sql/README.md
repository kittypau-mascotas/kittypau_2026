# SQL (features, calidad, reportes)

Convenciones:
- Preferir vistas para features derivadas (ej. `*_log`) mientras el schema base no las persista.
- Mantener queries de calidad (missing rate, duplicados, frescura) reutilizables por admin.

