<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        tools:replace="android:usesCleartextTraffic"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>

        <!-- Widget de Android — Knowledge/29_Specs/010-widget-android-hero.
             `exported="true"` es obligatorio para receivers de AppWidget (el
             sistema operativo, fuera del proceso de la app, es quien manda
             el broadcast APPWIDGET_UPDATE). -->
        <receiver
            android:name=".widget.KittypauHeroWidgetReceiver"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/kittypau_hero_widget_info" />
        </receiver>

        <!-- Configuration Activity — lanzada automático por Android después
             de bindear el widget (research.md Decisión 2), tanto si vino del
             selector manual como de requestPinAppWidget() (KittypauWidgetPlugin). -->
        <activity
            android:name=".widget.WidgetPetConfigActivity"
            android:exported="true"
            android:theme="@style/AppTheme"
            android:label="Elegir mascota para el widget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <!-- Trigger de refresco del widget por push (tasks.md T022, opcional/
             no bloqueante). FCM solo entrega a UN FirebaseMessagingService por
             app — se reemplaza el que trae @capacitor/push-notifications
             (mismo android:name exacto, ver
             node_modules/@capacitor/push-notifications/android/src/main/AndroidManifest.xml)
             por KittypauMessagingService, que lo EXTIENDE y llama
             super.onMessageReceived() primero — el push "comió"/"le sirvieron"
             existente sigue funcionando igual, solo se agrega el chequeo del
             data-flag nuevo. -->
        <service
            android:name="com.capacitorjs.plugins.pushnotifications.MessagingService"
            tools:node="remove" />
        <service
            android:name=".KittypauMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
