## Raspberry Bridge env (example)
## Copy to .env.bridge.local on the bridge host

# HiveMQ / MQTT
MQTT_HOST=broker.hivemq.com
MQTT_PORT=8883
MQTT_USER=your_mqtt_user
MQTT_PASS=your_mqtt_password
MQTT_TLS=true
MQTT_TOPIC=+/SENSORS

# Backend (Vercel API)
API_BASE_URL=https://kittypau-app.vercel.app
WEBHOOK_PATH=/api/mqtt/webhook
WEBHOOK_TOKEN=replace_with_MQTT_WEBHOOK_SECRET
HEARTBEAT_PATH=/api/bridge/heartbeat
HEARTBEAT_TOKEN=replace_with_BRIDGE_HEARTBEAT_SECRET

# Optional runtime
LOG_LEVEL=info
RETRY_BACKOFF_MS=1000
POLL_INTERVAL_MS=0
